name: expense_tracker_env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pandas
  - matplotlib
  - seaborn
  - pip
  - pip:
      - ydata-profiling
      - jupyter
      - scikit-learn
      - plotly
